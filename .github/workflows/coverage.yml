name: Coverage
on:
  push:
    branches: master

jobs:
  coverage:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['12.x']
    steps:
      - uses: codecov/codecov-action@v1
