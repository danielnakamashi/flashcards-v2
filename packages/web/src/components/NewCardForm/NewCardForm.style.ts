import { makeStyles, createStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() =>
  createStyles({
    buttonContainer: {
      height: '100%',
    },
  }),
);

export { useStyles };
